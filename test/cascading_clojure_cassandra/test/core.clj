(ns cascading-clojure-cassandra.test.core
  (:use [cascading-clojure-cassandra.core] :reload)
  (:use [clojure.test]))

(deftest replace-me ;; FIXME: write
  (is false "No tests have been written."))
